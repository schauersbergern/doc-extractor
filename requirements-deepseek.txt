# Modus 3: DeepSeek OCR 2
# System-Abhängigkeiten (separat installieren):
#   macOS: brew install --cask libreoffice && brew install poppler
#   Ubuntu: sudo apt install libreoffice poppler-utils
# Laufzeit-Hinweis:
#   DeepSeek OCR 2: Linux + NVIDIA/CUDA, Python 3.12/3.13 (nicht 3.14)
python-pptx>=0.6.23
Pillow>=10.0.0
pdf2image>=1.17.0

# PyTorch + Transformers
torch>=2.6.0
transformers==4.46.3
tokenizers==0.20.3
einops
addict
easydict

# Flash Attention (optional, aber empfohlen)
# pip install flash-attn==2.7.3 --no-build-isolation

# 4-bit Quantisierung (optional)
# bitsandbytes

# Alternative: vLLM statt Transformers (schneller)
# pip install -U vllm --pre --extra-index-url https://wheels.vllm.ai/nightly
